 
 <div class="page-title" style="background-image: url(../images/page-title.png)">
        <h1>Detalle</h1>
</div>

    <div class="container "style="margin-top: 1px;">
      
     <div class="row" style="margin-right: 10px; margin-top:20px; color: #494d59;
    font-family: Source Sans Pro Regular,sourcesanspro,sans-serif;
    font-size: 1.3rem;
    letter-spacing: .06rem;">
           <div class="col-md-2"></div>
       
        {{#each detalle}}
                    
                      
                    <div class="col-md-4">
                      <img src="../{{imagen}}"></img>
                    </div>
                    <div class="col-md-4" >
                      <div class="row" >
                       <h1>{{nombre}}</h1>
                       <h1>${{precio_venta}}.00</h1>
                      </div>
                      <div class="row" style="border-top-width: 1px; border-color: #e3ded1; border-top-style: solid;">
                       <h2>{{marca}}</h2>
                       <h2>{{material}}</h2>
                        <form action="/carrito/agregar/{{id}}" method="POST">
                       <h2>Tallas</h2>
                       
                           <select id="talla" name="talla" onchange="obtenerT();">
                                <option value="0">Seleccione</option>
                                 {{#each ../talla}}
                                <option value="{{id}}">{{name}}</option>
                               
                                {{/each}}
                             </select>
                        
                    </div>
                    <div class="row" style="margin:10px;">
                      <div class="col-sm-6">
                           <select id="cantidad" name="cantidad" onchange="obtener();">
                                <option value="volvo">Cantidad</option>
                                <option value="1">1</option>
                               <option value="2">2</option>
                                <option value="3">3</option>
                             </select>
                     </div>
                       <div class="col-sm-6">
                          <button class="btm btn-primary"   type="submit" >Añadir a la bolsa</button>
                      </div>
                    </div>
                    </form>
                    

                    
                    </div>
    
          {{/each}}
       </div>
    </div>
<script type="text/javascript">
  function ShowSelected()
  {
    /* Para obtener el valor */
   var cantidad= document.getElementById("cantidad").value;
    var talla= document.getElementById("talla").value;
    return parseInt(cantidad,talla);
 

   }
</script>